{{#partial "content"}}

    <div class="well">
        <p>{{teams.title}}:</p>

        <p>{{teams.description}}</p>

        <ul>{{member}}</ul>

        <p>
           <a href="/teams/{{teams.id}}/update">Edit this entry</a>
            <br>
           <a href="/teams/{{teams.id}}/add-member">Add Team Member</a>

           <a href="/teams/{{teams.id}}/delete">Delete this entry (this cannot be undone)</a>
        </p>
    </div>
{{/partial}}
{{> layout.hbs}}